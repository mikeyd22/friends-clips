# Friends clipping projects

Each source video is a separate project named with the request date and source title

`YYYY-MM-DD - Source Video Title`

Open the source project's `deliverables` folder to find its finished clips

Do not create additional source projects inside another project's `runs` folder Future Friends sources belong directly under this folder and must be added to `project.yaml`
